import sys
import math

def get_change(m):
    x=math.floor(m/10)
    y=math.floor((m-x*10)/5)
    z=m-(x*10)-(5*y)
    ans=x+y+z
    return ans

if __name__ == '__main__':
    m = int(sys.stdin.read())
    print(get_change(m))
